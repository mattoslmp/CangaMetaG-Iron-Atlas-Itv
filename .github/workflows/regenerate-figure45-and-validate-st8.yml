name: Regenerate Figure 4/5 and validate ST8 MTX

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "app.py"
      - "app_core.py"
      - "src/**"
      - "scripts/figures/**"
      - "tests/**"
      - "data/article_frozen_taxonomy_bacteria.json"
      - "data/article_frozen_taxonomy_archaea.json"
      - "tables/Supplementary_Table_8.xlsx"
      - ".github/workflows/regenerate-figure45-and-validate-st8.yml"

permissions:
  contents: read

concurrency:
  group: validate-figure45-st8-${{ github.ref }}
  cancel-in-progress: true

jobs:
  regenerate-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out required repository paths
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          lfs: false
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /.github/workflows/regenerate-figure45-and-validate-st8.yml
            /requirements.txt
            /app.py
            /app_core.py
            /src/
            /scripts/figures/
            /tests/
            /data/article_frozen_taxonomy_bacteria.json
            /data/article_frozen_taxonomy_archaea.json
            /data/final_publication_derived/
            /tables/Supplementary_Table_8.xlsx
            /outputs/final_publication_figures/Figure4_taxonomic_bacteria_genus_profiles.*
            /outputs/final_publication_figures/Figure5_taxonomic_archaea_genus_profiles.*
            /outputs/app_supplementary_figures/Figure4_taxonomic_bacteria_genus_profiles.*
            /outputs/app_supplementary_figures/Figure5_taxonomic_archaea_genus_profiles.*

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install focused dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            numpy pandas matplotlib plotly openpyxl pillow pypdf pytest

      - name: Regenerate Figures 4 and 5 from packaged inputs
        run: python scripts/figures/generate_article_figure45_final.py --root .

      - name: Validate all 189 KOs across the 12 MTX samples
        run: python scripts/figures/validate_st8_mtx_values.py --root .

      - name: Test Figure 4 and 5 final generator
        id: test_generator
        continue-on-error: true
        run: python -m pytest -q tests/test_article_figure45_final_generator.py

      - name: Test Figure 4 and 5 final application layer
        id: test_figure45_app
        continue-on-error: true
        run: python -m pytest -q tests/test_figure45_legend_below_final.py

      - name: Test KO Raw data and Z-score selector
        id: test_ko_selector
        continue-on-error: true
        run: python -m pytest -q tests/test_ko_heatmap_scale_selector.py

      - name: Test ST8 scientific contract
        id: test_st8_contract
        continue-on-error: true
        run: python -m pytest -q tests/test_st8_final_contract.py

      - name: Test ST8 MTX value classification
        id: test_st8_values
        continue-on-error: true
        run: python -m pytest -q tests/test_st8_mtx_value_diagnostics.py

      - name: Fail when any regression group failed
        if: always()
        run: |
          python - <<'PY'
          import os
          results = {
            "Figure 4/5 generator": "${{ steps.test_generator.outcome }}",
            "Figure 4/5 app layer": "${{ steps.test_figure45_app.outcome }}",
            "KO selector": "${{ steps.test_ko_selector.outcome }}",
            "ST8 contract": "${{ steps.test_st8_contract.outcome }}",
            "ST8 values": "${{ steps.test_st8_values.outcome }}",
          }
          print("Regression group outcomes:")
          for name, outcome in results.items():
            print(f"- {name}: {outcome}")
          failed = [name for name, outcome in results.items() if outcome != "success"]
          if failed:
            raise SystemExit("Failed regression groups: " + ", ".join(failed))
          PY

      - name: Verify generated deliverables
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          import pandas as pd

          required = [
            Path("outputs/final_publication_figures/Figure4_taxonomic_bacteria_genus_profiles.svg"),
            Path("outputs/final_publication_figures/Figure4_taxonomic_bacteria_genus_profiles.png"),
            Path("outputs/final_publication_figures/Figure4_taxonomic_bacteria_genus_profiles.pdf"),
            Path("outputs/final_publication_figures/Figure5_taxonomic_archaea_genus_profiles.svg"),
            Path("outputs/final_publication_figures/Figure5_taxonomic_archaea_genus_profiles.png"),
            Path("outputs/final_publication_figures/Figure5_taxonomic_archaea_genus_profiles.pdf"),
            Path("data/final_publication_derived/Figure45_final_generation_manifest.csv"),
            Path("data/final_publication_derived/ST8_MTX_value_validation.json"),
          ]
          missing = [str(path) for path in required if not path.is_file() or path.stat().st_size == 0]
          if missing:
            raise SystemExit("Missing generated files: " + ", ".join(missing))

          manifest = pd.read_csv("data/final_publication_derived/Figure45_final_generation_manifest.csv")
          if len(manifest) != 2 or set(manifest["figure"].astype(int)) != {4, 5}:
            raise SystemExit("Figure 4/5 generation manifest is incomplete")
          if not manifest["large_legend_layout"].astype(str).str.casefold().eq("true").all():
            raise SystemExit("Large bottom legends were not recorded for both figures")
          if not pd.to_numeric(manifest["legend_font_size_pt"]).ge(18).all():
            raise SystemExit("Figure legend font is smaller than 18 pt")
          if not pd.to_numeric(manifest["legend_title_font_size_pt"]).ge(21).all():
            raise SystemExit("Figure legend title is smaller than 21 pt")
          if manifest["scientific_values_changed"].astype(str).str.casefold().eq("true").any():
            raise SystemExit("Scientific values must not change during layout regeneration")

          validation = json.loads(Path("data/final_publication_derived/ST8_MTX_value_validation.json").read_text(encoding="utf-8"))
          if validation.get("status") != "PASS":
            raise SystemExit("ST8 MTX validation did not pass")
          if validation.get("KO_rows_classified") != 189:
            raise SystemExit("Not all 189 KOs were classified")
          if validation.get("resolved_mtx_column_count") != 12:
            raise SystemExit("Not all 12 MTX columns were resolved")
          if validation.get("source_issue_cells") != 0:
            raise SystemExit("Missing or non-numeric MTX source cells remain")
          PY
